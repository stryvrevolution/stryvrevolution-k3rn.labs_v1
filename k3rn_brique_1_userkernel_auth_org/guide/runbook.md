# Runbook — brique 1

1) Créer un projet Supabase (région Europe).
2) Exécuter `supabase/migrations/001_init.sql`.
3) `cp .env.example .env.local` puis renseigner les valeurs.
4) `npm install`
5) `npm run dev`
6) Tester :
   - `/auth`
   - `/onboarding`
   - `/app`
   - `/app/projects`
